@page "/"
@layout MainLayout
@inject IFilmService FilmService
@inject IApiUrlService ApiUrlService

<PageTitle>Главная - CinemaBlazor</PageTitle>

<div class="text-center py-5">
    <h1 class="display-4">🎬 Добро пожаловать в CinemaBlazor</h1>
    <p class="lead">Ваше любимое место для просмотра и управления фильмами</p>
    <hr class="my-4" />
</div>

@if (isLoading)
{
    <div class="text-center py-5">
        <div class="spinner-border text-primary" role="status">
            <span class="visually-hidden">Загрузка...</span>
        </div>
    </div>
}
else if (featuredFilms.Count > 0)
{
    <h3 class="mb-4">Популярные фильмы</h3>
    <div class="row row-cols-1 row-cols-md-3 g-4">
        @foreach (var film in featuredFilms.Take(3))
        {
            <div class="col">
                <div class="card h-100 shadow-sm">
                    @if (!string.IsNullOrEmpty(film.ImageUrl))
                    {
                        <img src="@ApiUrlService.GetImageUrl(film.ImageUrl)" class="card-img-top" alt="@film.Name" style="height: 200px; object-fit: cover;" />
                    }
                    else
                    {
                        <div class="bg-secondary text-white d-flex align-items-center justify-content-center"
                             style="height: 200px;">
                            <span class="bi bi-film" style="font-size: 3rem;"></span>
                        </div>
                    }
                    <div class="card-body">
                        <h5 class="card-title">@film.Name</h5>
                        <p class="card-text text-muted small">
                            @if (film.Genre != null)
                            {
                                <span class="badge bg-secondary me-1">@film.Genre.Name</span>
                            }
                        </p>
                        <a href="films/@film.Id" class="btn btn-outline-primary">Подробнее</a>
                    </div>
                </div>
            </div>
        }
    </div>
    <div class="text-center mt-4">
        <a href="films" class="btn btn-link">Все фильмы →</a>
    </div>
}

@code {
    private List<Film> featuredFilms = new();
    private bool isLoading = true;

    protected override async Task OnInitializedAsync()
    {
        featuredFilms = await FilmService.GetAllFilmsAsync("rating_desc");
        isLoading = false;
    }
}