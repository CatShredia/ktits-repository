<Window x:Class="test_application.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:test_application" xmlns:components="clr-namespace:test_application.Components"
        mc:Ignorable="d"
        Title="StartWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="80*"></RowDefinition>
            <RowDefinition Height="10*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="Gray">
            <components:MenuComponent></components:MenuComponent>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="15*"></ColumnDefinition>
                <ColumnDefinition Width="80*"></ColumnDefinition>
                <ColumnDefinition Width="15*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Column="0" Background="Green">
                <TextBlock Text="First Panel" Margin="10"></TextBlock>
            </Grid>
            <Grid Grid.Column="1" Background="White">
                <StackPanel Margin="10">
                    <TextBlock Text="Main Panel" HorizontalAlignment="Center" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

                    <Button Content="Кнопка" Margin="0,0,0,10"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Имя:" VerticalAlignment="Center" Margin="0,0,5,0" Width="auto"/>
                        <TextBox Width="200"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Дата:" VerticalAlignment="Center" Margin="0,0,5,0" Width="auto"/>
                        <DatePicker Width="200"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Студент:" VerticalAlignment="Center" Margin="0,0,5,0" Width="auto"/>
                        <CheckBox VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel>
                        <DataGrid x:Name="MyDataGrid" AutoGenerateColumns="False" Margin="10">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Имя" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Возраст" Binding="{Binding Age}" Width="*"/>
                                <DataGridTextColumn Header="Город" Binding="{Binding City}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>

                    <StackPanel>
                        <Image Source="/images/myfile.jpg" Width="200" Height="200" Margin="0,0,0,10"></Image>
                    </StackPanel>

                </StackPanel>
            </Grid>
            <Grid Grid.Column="2" Background="Red">
                <TextBlock Text="Last Panel" Margin="10"></TextBlock>
            </Grid>
        </Grid>
        <Grid Grid.Row="2" Background="Gray">
            <TextBlock Text="Footer" Margin="10" HorizontalAlignment="Center"></TextBlock>
        </Grid>
    </Grid>
</Window>
